// Public API of the map-explorer feature. The app layer only needs the map entry point
// (a client island that loads Leaflet with ssr:false).

export { default as MapApp } from "@/features/map-explorer/components/MapApp";
